# Інструкції для деплою Nexus

Детальні інструкції для розгортання проєкту Nexus на Vercel або Netlify.

## 🚀 Деплой на Vercel (рекомендовано)

### 1. Підготовка проєкту

1. Переконайтеся, що ваш код знаходиться в GitHub репозиторії
2. Перевірте, що всі залежності вказані в `package.json`
3. Переконайтеся, що проєкт збирається локально: `npm run build`

### 2. Створення проєкту в Vercel

1. Перейдіть на [vercel.com](https://vercel.com)
2. Увійдіть через GitHub акаунт
3. Натисніть "New Project"
4. Виберіть ваш репозиторій з Nexus
5. Налаштуйте проєкт:
   - **Framework Preset**: Next.js
   - **Root Directory**: `./` (за замовчуванням)
   - **Build Command**: `npm run build` (за замовчуванням)
   - **Output Directory**: `.next` (за замовчуванням)
   - **Install Command**: `npm install` (за замовчуванням)

### 3. Налаштування змінних оточення

В розділі "Environment Variables" додайте:

```env
AIRTABLE_API_KEY=patR0YRacAA6i5hns.d3e5abcc560a9f64265f6ec6b925529d16f3d41c0034110b6a048ee0bfb58f66
AIRTABLE_BASE_ID=app5OIIDRHnpG7rXt
```

### 4. Деплой

1. Натисніть "Deploy"
2. Дочекайтеся завершення збірки
3. Ваш сайт буде доступний за URL типу: `https://your-project.vercel.app`

### 5. Налаштування домену (опціонально)

1. В налаштуваннях проєкту перейдіть в "Domains"
2. Додайте ваш кастомний домен
3. Налаштуйте DNS записи згідно інструкцій Vercel

## 🌐 Деплой на Netlify

### 1. Підготовка проєкту

1. Переконайтеся, що ваш код знаходиться в GitHub репозиторії
2. Додайте файл `netlify.toml` в корінь проєкту:

```toml
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

### 2. Створення проєкту в Netlify

1. Перейдіть на [netlify.com](https://netlify.com)
2. Увійдіть через GitHub акаунт
3. Натисніть "New site from Git"
4. Виберіть ваш репозиторій
5. Налаштуйте збірку:
   - **Build command**: `npm run build`
   - **Publish directory**: `.next`

### 3. Налаштування змінних оточення

В розділі "Site settings" → "Environment variables" додайте:

```env
AIRTABLE_API_KEY=patR0YRacAA6i5hns.d3e5abcc560a9f64265f6ec6b925529d16f3d41c0034110b6a048ee0bfb58f66
AIRTABLE_BASE_ID=app5OIIDRHnpG7rXt
```

### 4. Деплой

1. Натисніть "Deploy site"
2. Дочекайтеся завершення збірки
3. Ваш сайт буде доступний за URL типу: `https://random-name.netlify.app`

## 🔧 Налаштування Airtable

### 1. Створення таблиці "Cards"

1. Увійдіть в [Airtable](https://airtable.com)
2. Створіть нову базу даних або використайте існуючу
3. Створіть таблицю "Cards" з наступними полями:

| Назва поля | Тип | Опис |
|------------|-----|------|
| Card Number | Single line text | Номер картки |
| Balance | Number | Баланс картки |
| Status | Single select | active/blocked |
| Linked Offer | Single line text | Пов'язаний оффер |
| Linked Expense | Single line text | Пов'язані витрати |
| AI Evaluation | Long text | AI оцінка картки |
| Automations | Long text | Налаштування автоматизацій |

### 2. Налаштування полів

Для поля "Status":
1. Виберіть тип "Single select"
2. Додайте опції: "active", "blocked"

### 3. Отримання API ключа

1. Перейдіть в [Airtable API](https://airtable.com/api)
2. Виберіть вашу базу даних
3. Скопіюйте:
   - **API Key**: `patR0YRacAA6i5hns.d3e5abcc560a9f64265f6ec6b925529d16f3d41c0034110b6a048ee0bfb58f66`
   - **Base ID**: `app5OIIDRHnpG7rXt`

## 🧪 Тестування після деплою

### 1. Перевірка основних функцій

1. **Головна сторінка**: Перевірте, що сайт завантажується
2. **Темна/світла тема**: Перевірте перемикання тем
3. **Адаптивність**: Перевірте на мобільних пристроях
4. **Airtable інтеграція**: Перевірте додавання карток

### 2. Тестування Airtable API

1. Відкрийте DevTools (F12)
2. Перейдіть на вкладку "Network"
3. Спробуйте додати нову картку
4. Перевірте, що запит до Airtable успішний

### 3. Перевірка помилок

1. Відкрийте DevTools → Console
2. Перевірте наявність помилок
3. Перевірте логи в Vercel/Netlify dashboard

## 🔒 Безпека

### 1. Захист API ключів

- ✅ Використовуйте змінні оточення
- ✅ Не комітьте `.env.local` в Git
- ✅ Обмежте доступ до API ключів

### 2. HTTPS

- ✅ Vercel та Netlify автоматично надають HTTPS
- ✅ Всі з'єднання захищені

### 3. CORS налаштування

Якщо виникають CORS помилки, додайте в `next.config.js`:

```javascript
module.exports = {
  async headers() {
    return [
      {
        source: '/api/:path*',
        headers: [
          { key: 'Access-Control-Allow-Origin', value: '*' },
          { key: 'Access-Control-Allow-Methods', value: 'GET,POST,PUT,DELETE,OPTIONS' },
          { key: 'Access-Control-Allow-Headers', value: 'Content-Type' },
        ],
      },
    ]
  },
}
```

## 📊 Моніторинг

### 1. Vercel Analytics

1. В налаштуваннях проєкту увімкніть Analytics
2. Отримайте детальну статистику відвідувань

### 2. Netlify Analytics

1. В налаштуваннях сайту увімкніть Analytics
2. Переглядайте статистику в реальному часі

### 3. Логи помилок

- **Vercel**: Functions → Logs
- **Netlify**: Site settings → Functions → Logs

## 🚀 Оптимізація продуктивності

### 1. Зображення

```javascript
// Використовуйте Next.js Image компонент
import Image from 'next/image'

<Image
  src="/logo.png"
  alt="Logo"
  width={100}
  height={100}
  priority
/>
```

### 2. Кешування

```javascript
// В next.config.js
module.exports = {
  async headers() {
    return [
      {
        source: '/static/:path*',
        headers: [
          {
            key: 'Cache-Control',
            value: 'public, max-age=31536000, immutable',
          },
        ],
      },
    ]
  },
}
```

### 3. Bundle аналіз

```bash
# Аналіз розміру bundle
npm run build
npx @next/bundle-analyzer
```

## 🔄 CI/CD

### 1. Автоматичний деплой

- **Vercel**: Автоматично деплоїть при push в main branch
- **Netlify**: Автоматично деплоїть при push в main branch

### 2. Preview деплої

- **Vercel**: Автоматично створює preview для pull requests
- **Netlify**: Автоматично створює preview для pull requests

## 🆘 Вирішення проблем

### Помилка "Module not found"

```bash
# Перевірте, що всі залежності встановлені
npm install

# Очистіть кеш
npm run build -- --no-cache
```

### Помилка "Environment variables not found"

1. Перевірте, що змінні додані в Vercel/Netlify
2. Перезапустіть деплой
3. Перевірте назви змінних (чутливі до регістру)

### Помилка "Airtable API error"

1. Перевірте API ключ та Base ID
2. Перевірте права доступу до таблиці
3. Перевірте структуру полів в Airtable

### Помилка "Build failed"

1. Перевірте логи збірки
2. Перевірте синтаксис коду
3. Перевірте залежності в `package.json`

## 📞 Підтримка

Якщо у вас є проблеми з деплоєм:

1. Перевірте логи в Vercel/Netlify dashboard
2. Перевірте Console в браузері
3. Створіть Issue в GitHub з детальним описом проблеми

---

**Успішного деплою!** 🚀 