# Налаштування KasmWeb Сервера

## 🚀 Швидкий старт

### 1. Налаштування змінних середовища

Скопіюй файл конфігурації:
```bash
cp kasm-config.example.env .env.local
```

Відредагуй `.env.local` з твоїми налаштуваннями:
```env
# URL твого KasmWeb сервера
NEXT_PUBLIC_KASM_SERVER_URL=https://kasm.your-domain.com

# API ключі (заміни на свої)
NEXT_PUBLIC_KASM_API_KEY=dcV08sbYEhM1
NEXT_PUBLIC_KASM_API_SECRET=dl3vOjcoAsvFE65GYIBjOwieFrnRMvz7

# Налаштування сесій
NEXT_PUBLIC_KASM_MAX_SESSIONS=5
NEXT_PUBLIC_KASM_SESSION_TIMEOUT=30
NEXT_PUBLIC_KASM_AUTO_CLEANUP=true
```

### 2. Перезапуск сервера

```bash
npm run dev
```

### 3. Тестування підключення

1. Перейди до `/settings`
2. Знайди секцію "KasmWeb Налаштування"
3. Натисни "Тестувати підключення"

## 🔧 Типи KasmWeb серверів

### Локальний сервер
```env
NEXT_PUBLIC_KASM_SERVER_URL=https://192.168.1.100:6901
```

### VPS сервер
```env
NEXT_PUBLIC_KASM_SERVER_URL=https://your-server-ip:6901
```

### Хмарний сервер
```env
NEXT_PUBLIC_KASM_SERVER_URL=https://kasm.your-domain.com
```

## 📋 Перевірка конфігурації

### Валідація налаштувань
Система автоматично перевіряє:
- ✅ Наявність API ключів
- ✅ Правильність URL сервера
- ✅ Доступність сервера

### Тестування ендпоінтів
- **Сесії** - перевірка створення/керування сесіями
- **Образів** - перевірка доступних браузерів
- **Серверів** - перевірка стану серверів

## 🛠 Розв'язання проблем

### Помилка "API_KEY не налаштований"
```env
# Перевір, що в .env.local є:
NEXT_PUBLIC_KASM_API_KEY=твій_реальний_ключ
NEXT_PUBLIC_KASM_API_SECRET=твій_реальний_секрет
```

### Помилка "SERVER_URL не налаштований"
```env
# Перевір URL сервера:
NEXT_PUBLIC_KASM_SERVER_URL=https://реальний_сервер.com
```

### Помилка підключення до сервера
1. Перевір, чи запущений KasmWeb сервер
2. Перевір правильність URL
3. Перевір налаштування файрвола
4. Перевір SSL сертифікати

### Помилка авторизації
1. Перевір правильність API ключів
2. Перевір права доступу користувача
3. Перевір налаштування CORS

## 🔐 Безпека

### Рекомендації
- ✅ Використовуй HTTPS для всіх з'єднань
- ✅ Регулярно змінюй API ключі
- ✅ Обмежуй доступ до сервера по IP
- ✅ Використовуй сильні паролі

### Змінні середовища
```env
# Для продакшену використовуй:
NODE_ENV=production
NEXT_PUBLIC_KASM_SERVER_URL=https://secure-kasm.your-domain.com
```

## 📊 Моніторинг

### Логи
Всі операції логуються в консолі браузера:
```javascript
// Приклад логу
console.log('KasmWeb API request failed:', error)
```

### Метрики
Відстежуй:
- Кількість активних сесій
- Час відповіді API
- Помилки підключення
- Використання ресурсів

## 🔄 Оновлення

### Оновлення конфігурації
1. Зупини сервер розробки
2. Онови `.env.local`
3. Перезапусти сервер
4. Протестуй підключення

### Оновлення API ключів
1. Згенеруй нові ключі в KasmWeb
2. Онови `.env.local`
3. Перезапусти додаток

## 📞 Підтримка

### Корисні команди
```bash
# Перевірка змінних середовища
echo $NEXT_PUBLIC_KASM_SERVER_URL

# Перезапуск сервера
npm run dev

# Очищення кешу
npm run build && npm start
```

### Контакти
- Документація KasmWeb: https://www.kasmweb.com/docs/
- GitHub репозиторій: https://github.com/kasmtech/KasmVNC
- Підтримка: support@kasmweb.com

## 🎯 Наступні кроки

Після успішного налаштування:
1. Створи образи браузерів у KasmWeb
2. Налаштуй браузерні профілі
3. Протестуй створення сесій
4. Налаштуй автоматизацію

---

**Примітка**: Завжди тестуй налаштування в тестовому середовищі перед використанням у продакшені. 