# KasmWeb Інтеграція

## Огляд

KasmWeb - це потужна платформа для створення антидетект браузерів з автоматизацією дій. Ця інтеграція дозволяє керувати браузерними сесіями безпосередньо з системи Nexus.

## Можливості

### 🚀 Основні функції
- **Автоматичний запуск браузерів** для кожного аккаунта
- **Унікальні браузерні профілі** з різними відбитками
- **Керування сесіями** (запуск, зупинка, видалення)
- **Інтеграція з існуючою системою** аккаунтів
- **Підтримка проксі** та геолокації

### 🎯 Платформи
- **Facebook** - для рекламних кампаній
- **Google Ads** - для Google реклами
- **Telegram** - для комунікації та ботів
- **TikTok** - для TikTok реклами
- **Instagram** - для Instagram реклами

## Налаштування

### 1. Конфігурація KasmWeb

Відредагуйте файл `src/lib/kasmWeb.ts`:

```typescript
export const kasmWebClient = new KasmWebClient(
  'dcV08sbYEhM1', // Ваш API ключ
  'dl3vOjcoAsvFE65GYIBjOwieFrnRMvz7', // Ваш API секрет
  'https://your-kasm-server.com' // URL вашого KasmWeb сервера
)
```

### 2. Образів браузерів

Створіть або налаштуйте образи браузерів у KasmWeb:
- `chrome-120` - Chrome 120 для загального використання
- `firefox-120` - Firefox 120 для альтернативних сценаріїв
- `edge-120` - Edge 120 для Microsoft платформ

### 3. Ліміти та обмеження

**Поточні ліміти:**
- Максимум 5 одночасних браузерних сесій
- Автоматичне очищення неактивних сесій через 30 хвилин
- Обмеження на кількість запитів до API

## Використання

### Запуск браузера для аккаунта

1. Перейдіть до розділу "Аккаунти"
2. Відкрийте картку аккаунта
3. Знайдіть секцію "Антидетект Браузер"
4. Натисніть "Запустити браузер"

### Керування сесіями

```typescript
// Запуск браузера
const session = await launchAccountBrowser(account)

// Зупинка браузера
await stopAccountBrowser(accountId)

// Отримання URL для підключення
const url = await getSessionUrl(sessionId)
```

### Браузерні профілі

Кожен аккаунт може мати унікальний браузерний профіль:

```typescript
const browserProfile = {
  userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36...',
  screenResolution: '1920x1080',
  timezone: 'Europe/Kiev',
  language: 'uk-UA,uk;q=0.9,en;q=0.8',
  geolocation: '50.4501,30.5234'
}
```

## API Endpoints

### GET /api/kasmweb
- `?action=sessions` - отримати список сесій
- `?action=images` - отримати список образів
- `?action=servers` - отримати список серверів

### POST /api/kasmweb
```json
{
  "action": "create_session",
  "imageId": "chrome-120",
  "name": "Facebook-Account-123",
  "options": {
    "browser_profile": {...},
    "proxy_config": {...}
  }
}
```

## Компоненти

### BrowserManager
Основний компонент для керування браузерними сесіями:
- Відображення статусу сесії
- Кнопки запуску/зупинки
- Інформація про профіль браузера

### KasmWebProvider
Провайдер контексту для керування станом:
- Список активних сесій
- Методи для роботи з API
- Обробка помилок

## Автоматизація

### Сценарії використання

1. **Реєстрація аккаунтів**
   - Автоматичний запуск браузера
   - Заповнення форм реєстрації
   - Верифікація email/телефону

2. **Рекламні кампанії**
   - Створення рекламних кампаній
   - Налаштування таргетингу
   - Моніторинг результатів

3. **Парсинг даних**
   - Збір інформації з сайтів
   - Аналіз конкурентів
   - Моніторинг цін

### Приклад автоматизації

```typescript
// Автоматична реєстрація Facebook аккаунта
async function registerFacebookAccount(accountData) {
  const session = await launchAccountBrowser(accountData)
  
  // Навігація до Facebook
  await navigateTo(session, 'https://facebook.com')
  
  // Заповнення форми реєстрації
  await fillRegistrationForm(session, accountData)
  
  // Підтвердження email
  await confirmEmail(session, accountData.email)
  
  return session
}
```

## Безпека

### Рекомендації
- Використовуйте різні IP адреси для кожного аккаунта
- Регулярно змінюйте браузерні профілі
- Не використовуйте однакові дані для різних аккаунтів
- Моніторте активність аккаунтів

### Проксі налаштування

```typescript
const proxyConfig = {
  proxy_type: 'http',
  proxy_host: 'proxy.example.com',
  proxy_port: 8080,
  proxy_username: 'user',
  proxy_password: 'pass'
}
```

## Моніторинг

### Метрики для відстеження
- Кількість активних сесій
- Час роботи браузерів
- Успішність операцій
- Помилки та виключення

### Логування
Всі операції з браузерами логуються для аналізу:
- Запуск/зупинка сесій
- Помилки підключення
- Використання ресурсів

## Розширення

### Додавання нових платформ

1. Додайте нову платформу в типи:
```typescript
platform: 'Facebook' | 'Google' | 'TikTok' | 'Instagram' | 'YouTube' | 'Twitter' | 'LinkedIn' | 'NewPlatform'
```

2. Налаштуйте образ браузера:
```typescript
const getImageForPlatform = (platform: string): string => {
  switch (platform.toLowerCase()) {
    case 'newplatform':
      return 'chrome-120' // або спеціальний образ
    // ...
  }
}
```

### Кастомні автоматизації

Створіть власні скрипти автоматизації для специфічних завдань:

```typescript
// Приклад кастомної автоматизації
export class CustomAutomation {
  constructor(private session: KasmSession) {}
  
  async performCustomAction() {
    // Ваша логіка автоматизації
  }
}
```

## Підтримка

### Відомі проблеми
- Затримки при запуску браузерів (до 30 секунд)
- Обмеження на кількість одночасних сесій
- Залежність від стабільності KasmWeb сервера

### Плани розвитку
- [ ] Підтримка мобільних браузерів
- [ ] Розширена автоматизація
- [ ] Інтеграція з Selenium
- [ ] Підтримка Puppeteer
- [ ] Розподілене керування сесіями

## Контакти

Для підтримки та питань щодо KasmWeb інтеграції звертайтеся до команди розробки. 