# 🔧 Налаштування підключення до KasmWeb

## 📋 Що потрібно налаштувати:

### 1. **URL твого KasmWeb сервера**
Заміни в файлі `src/lib/kasmConfig.ts`:

```typescript
// Заміни на URL твого KasmWeb сервера
SERVER_URL: process.env.NEXT_PUBLIC_KASM_SERVER_URL || 'https://kasm.your-domain.com',
```

**Приклади:**
- `https://kasm.example.com`
- `http://192.168.1.100:6901`
- `https://kasm.mydomain.com`

### 2. **API ключі**
Перевір, чи правильні API ключі:

```typescript
API_KEY: process.env.NEXT_PUBLIC_KASM_API_KEY || 'dcV08sbYEhM1',
API_SECRET: process.env.NEXT_PUBLIC_KASM_API_SECRET || 'dl3vOjcoAsvFE65GYIBjOwieFrnRMvz7',
```

### 3. **Environment variables**
Створи файл `.env.local` в корені проекту:

```env
# KasmWeb Configuration
NEXT_PUBLIC_KASM_SERVER_URL=https://kasm.your-domain.com
NEXT_PUBLIC_KASM_API_KEY=dcV08sbYEhM1
NEXT_PUBLIC_KASM_API_SECRET=dl3vOjcoAsvFE65GYIBjOwieFrnRMvz7
```

## 🚀 Як протестувати:

### 1. **Перейдіть на сторінку браузерних профілів**
`http://localhost:3000/browser-profiles`

### 2. **Натисніть "Імпорт з KasmWeb"**
Зелена кнопка з іконкою завантаження

### 3. **Перевірте підключення**
- Побачите статистику: кількість сесій, образів, серверів
- Якщо є помилки - перевірте URL та API ключі

### 4. **Імпортуйте профіль**
- Виберіть твій Chrome профіль
- Натисніть "Імпортувати"
- Профіль з'явиться в списку

## 🔍 Troubleshooting:

### **Помилка підключення:**
1. Перевір URL сервера
2. Перевір API ключі
3. Перевір мережеве з'єднання

### **Не бачить сесії:**
1. Перевір, чи запущений KasmWeb сервер
2. Перевір, чи є активні сесії
3. Перевір права доступу API

### **Помилка CORS:**
1. Налаштуй CORS на KasmWeb сервері
2. Додай домен `localhost:3000` до дозволених

## 📞 Підтримка:

Якщо щось не працює, надай:
1. URL твого KasmWeb сервера
2. Помилки з консолі браузера (F12)
3. Помилки з терміналу

---

**Готово до тестування!** 🚀 